[pytest]
addopts = -ra
testpaths = tests
markers =
    unit: fast tests that do not require Blender (bpy)
    blender: tests that require Blender (bpy) and should be run inside Blender
    e2e: long end-to-end checks
    slow: long runtime
