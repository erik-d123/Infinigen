--- a/lidar/lidar_config.py
+++ b/lidar/lidar_config.py
@@ -49,19 +49,12 @@ class LidarConfig:
     def __post_init__(self):
-        # Apply preset-derived fields if available
-        p = LIDAR_PRESETS.get(self.preset, None)
-        if p:
-            # Only override if caller did not specify explicitly
-            if "rings" not in self.extras and (self.rings is None or self.rings <= 0):
-                self.rings = int(p["rings"])
-            else:
-                # keep provided rings
-                self.rings = int(self.rings)
-            if "max_range" not in self.extras and (self.max_range is None or self.max_range <= 0):
-                self.max_range = float(p["max_range"])
+        # Apply preset-derived fields unconditionally so OS1-128 uses 128 rings, etc.
+        p = LIDAR_PRESETS.get(self.preset)
+        if p:
+            self.rings = int(p["rings"])
+            self.max_range = float(p["max_range"])
         # Force azimuth override if provided
         if self.force_azimuth_steps is not None:
             self.azimuth_steps = int(self.force_azimuth_steps)
--- a/lidar/lidar_raycast.py
+++ b/lidar/lidar_raycast.py
@@ -274,7 +274,7 @@ def perform_raycasting(
     # Exposure mapping to U8
     auto = bool(getattr(cfg, "auto_expose", False))
-    global_scale = float(getattr(cfg, "global_scale", 1.0)) / 255.0
+    global_scale = float(getattr(cfg, "global_scale", 1.0))
     target_pct = float(getattr(cfg, "target_percentile", 95.0))
     target_u8 = float(getattr(cfg, "target_intensity", 200.0))
 
@@ -283,7 +283,7 @@ def perform_raycasting(
     if auto and np.count_nonzero(pos_mask) >= 4:
         scale = _percentile_scale(inten_raw[pos_mask], target_pct, target_u8)
     else:
-        scale = global_scale
+        scale = global_scale  # manual gain; intensity_u8 = clip(raw * scale * 255)
     inten_u8 = np.clip(np.round(inten_raw * scale * 255.0), 0, 255).astype(np.uint8)
